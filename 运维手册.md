# WebPtzSateTracker SSH 运维手册

> 云台（YD3040 / Pelco-D）卫星跟踪系统 · 服务器操作与测试指南
> 服务器：`192.168.x.x`　SSH 用户/密码：`<用户> / <密码>`　服务：`ptz.service`

---

## 1. SSH 连接

```bash
ssh <用户>@192.168.x.x        # 密码 <密码>
```

日常操作几乎都要 `sudo`（密码同为 <密码>）：

```bash
echo '<密码>' | sudo -S systemctl status ptz.service   # 查看服务状态
```

---

## 2. 服务管理

```bash
# 重启服务（部署代码后必做）
echo '<密码>' | sudo -S systemctl restart ptz.service

# 查看状态（是否 running、PID、CPU/内存）
echo '<密码>' | sudo -S systemctl status ptz.service

# 停止 / 启动
echo '<密码>' | sudo -S systemctl stop ptz.service
echo '<密码>' | sudo -S systemctl start ptz.service
```

### 查看日志

```bash
# 最近 50 行
echo '<密码>' | sudo -S journalctl -u ptz.service -n 50 --no-pager

# 实时跟踪（Ctrl+C 退出）
echo '<密码>' | sudo -S journalctl -u ptz.service -f

# 按时间段查
echo '<密码>' | sudo -S journalctl -u ptz.service --since '2026-09-11 06:00:00' --until '2026-09-11 07:00:00'

# 只看跟踪/异常关键日志
echo '<密码>' | sudo -S journalctl -u ptz.service -n 200 | grep -E '\[track\]|\[enc\]|\[filter\]|\[udp\]|\[reset\]'
```

---

## 3. 部署代码

代码在服务器 `/home/<用户>/ptz/`：

| 路径 | 说明 |
|------|------|
| `/home/<用户>/ptz/backend/main.py` | 后端主程序（Flask + waitress） |
| `/home/<用户>/ptz/backend/satellite.py` | 卫星跟踪逻辑 |
| `/home/<用户>/ptz/backend/lotw.py` | LoTW 日志拉取 + VUCC 网格统计 |
| `/home/<用户>/ptz/backend/auth.py` | 用户认证 |
| `/home/<用户>/ptz/frontend/` | 网页前端（index.html / satellite.js） |
| `/home/<用户>/ptz/users.json` | 网页登录账号（scrypt 哈希） |
| `/home/<用户>/ptz/config.json` | 配置（云台速度、TLE 源、LoTW 凭据、VUCC 频段等） |
| `/home/<用户>/ptz/tle_cache.json` | TLE 卫星数据缓存 |
| `/home/<用户>/ptz/lotw_cache.json` | LoTW 拉取缓存（bands/qso_count/since） |

本地 `scp` 上传后重启：

```bash
scp main.py <用户>@192.168.x.x:/home/<用户>/ptz/backend/main.py
ssh <用户>@192.168.x.x "echo '<密码>' | sudo -S systemctl restart ptz.service"
```

> 前端文件（index.html 等）是静态文件，上传后**无需重启**，刷新浏览器即可。

---

## 4. 常用 API 测试

服务端口：**<Web端口>**（网页/API）　**<UDP端口>**（ESP8266 UDP 上报）。
登录后带 cookie 测试：

```bash
# 登录（网页账号 <呼号>，密码见 users.json 或已重置为 <密码>）
curl -s -c /tmp/jar -X POST http://127.0.0.1:<Web端口>/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"<呼号>","password":"<密码>"}'

# 以下命令统一加 -b /tmp/jar 保持登录
J="-b /tmp/jar"
```

### 4.1 状态查询

```bash
curl -s $J http://127.0.0.1:<Web端口>/api/status          # 服务/串口/UDP/暂停/复位状态
curl -s $J http://127.0.0.1:<Web端口>/api/encoder         # AS5600 编码器实时值(含固件版本 ver)
curl -s $J http://127.0.0.1:<Web端口>/api/position        # 云台推算位置
curl -s $J http://127.0.0.1:<Web端口>/api/sat/track/status # 是否在跟踪/跟踪哪颗
curl -s $J http://127.0.0.1:<Web端口>/api/seriallog       # 串口收发日志
curl -s $J http://127.0.0.1:<Web端口>/api/logs             # 调试日志面板数据
```

### 4.2 手动驱动测试（范例）

```bash
# 按住方向（持续转，直到发 stop）
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/move/right
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/move/stop

# 点动脉冲：向右转 500ms（俯仰用 up/down）
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/pulse \
  -H 'Content-Type: application/json' -d '{"dir":"right","ms":500}'
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/pulse \
  -H 'Content-Type: application/json' -d '{"dir":"up","ms":300}'

# 转到指定位置
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/move/to \
  -H 'Content-Type: application/json' -d '{"pan":180,"tilt":45}'
```

### 4.3 复位与模式

```bash
# 复位：水平找光电零位 + 俯仰归零（约 20~40s）
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/reset

# 紧急刹车（暂停所有转动）/ 恢复
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/pause -H 'Content-Type: application/json' -d '{"paused":true}'
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/pause -H 'Content-Type: application/json' -d '{"paused":false}'

# 开环控制模式（不信 AS5600，只用光电零位 + 航位推算）
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/mode/open_loop -H 'Content-Type: application/json' -d '{"enabled":true}'
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/mode/open_loop -H 'Content-Type: application/json' -d '{"enabled":false}'

# 防缠绕复位（按累计转动反向回退）
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/anti_tangle_reset
```

### 4.4 卫星跟踪测试

```bash
# 查询 TLE 卫星（搜索）
curl -s $J "http://127.0.0.1:<Web端口>/api/sat/search?q=ISS"

# 收藏列表
curl -s $J http://127.0.0.1:<Web端口>/api/sat/favorites

# 开始跟踪（NORAD 编号）
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/sat/track/25544

# 停止跟踪
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/sat/track/stop

# 卫星实时位置（方位/仰角）
curl -s $J http://127.0.0.1:<Web端口>/api/sat/position/25544
```

### 4.5 光电校零与测速

```bash
# 光电自动校零+测速（云台自动转一圈，光电触发两次完成）
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/encoder/photocalib \
  -H 'Content-Type: application/json' -d '{"mode":"auto"}'

# 手动设当前位置为 0° 基准
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/encoder/setzero
```

### 4.6 LoTW / VUCC

```bash
# 查看拉取状态 + VUCC 缓存（bands 按频段列出已确认网格）
curl -s $J http://127.0.0.1:<Web端口>/api/lotw

# 触发拉取（body 可覆盖呼号/密码，留空用 config.json 配置）
curl -s $J -X POST http://127.0.0.1:<Web端口>/api/lotw/fetch \
  -H 'Content-Type: application/json' -d '{"callsign":"<呼号>","password":"xxx"}'

# 查看缓存文件
cat /home/<用户>/ptz/lotw_cache.json
```

> **LoTW 拉取要点**：按频段分拉（6M/2M/70CM），增量窗口从 400 天前起，每次后推进。
> 全量查询会收到 LoTW **503 限流**；缺 `qso_qsldetail=yes` 时 ADIF 无网格字段。首次拉取约 2 分钟，之后增量几秒。前端设置面板可配置并触发。

---

## 5. 速度校准流程（换新云台/传动比时）

1. 网页"速度校准"卡片，或直接调 `/api/calibrate`：
   ```bash
   curl -s $J -X POST http://127.0.0.1:<Web端口>/api/calibrate \
     -H 'Content-Type: application/json' -d '{"direction":"right","duration":5000}'
   ```
2. 实测转过角度 → 更新 `config.json` 的 `pan_speed_dps` / `tilt_*_speed_dps`
3. 重启服务生效

> 速度标定直接影响航位推算精度（开环模式 + 闭环断流推算都依赖它）。

---

## 6. ESP8266 传感器

- IP：`192.168.x.x`（DHCP，可能有变，以服务器日志为准）
- 协议：UDP 广播 → 服务器 `<UDP端口>` 端口，20Hz 上报角度 + 光电零位事件
- 固件版本：`/api/encoder` 返回 `ver` 字段

### 固件 OTA 更新

```bash
# HTTP OTA：浏览器或 curl 上传 .bin（口令 <OTA口令>）
curl -s -m 120 -F "token=<OTA口令>" \
  -F "sketch=@固件.bin" \
  http://192.168.x.x:<OTA端口>/update
# 返回 "OK. Rebooting..." 即成功
```

> 网页入口：`http://192.168.x.x:<OTA端口>`（上传页面）；ArduinoOTA 端口 <OTA端口>（IDE 直传）。

---

## 7. 常见问题排查

| 现象 | 排查步骤 |
|------|----------|
| 服务起不来 | `journalctl -u ptz.service -n 30` 看 traceback；`python3 -m py_compile main.py` 查语法 |
| ESP32 显示离线 | `curl /api/encoder` 看 `time`/`now` 差；串口日志是否报 `UnboundLocalError` |
| 跟踪乱转 | 日志看 `[track] SLOW/watchdog`、`[encoder] 锚定`、`[filter] 忽略毛刺/基准更新` |
| 卫星列表空 | `/api/sat/tle` 看 count；`tle_cache.json` 是否有效；网络能否访问 TLE 源 |
| 网页登录不了 | `users.json` 账号；密码已重置为 <密码>；`curl /api/auth/login` 测 |
| LoTW 拉取无结果/卡死 | `/api/lotw` 看 status；确认 config.json 有 `lotw_callsign/lotw_password`；LoTW 对全量查询返回 503，必须增量窗口 |
| 页面空白（地图/列表全不显示） | 浏览器控制台看 JS 报错；satellite.js 更新后强制刷新（Ctrl+Shift+R）清缓存 |