# WebPtzSateTracker SSH 运维手册

> 云台（YD3040 / Pelco-D）卫星跟踪系统 · 服务器操作与测试指南
> 服务器：`192.168.31.70`　SSH 用户/密码：`aap / 8023`　服务：`ptz.service`

---

## 1. SSH 连接

```bash
ssh aap@192.168.31.70        # 密码 8023
```

日常操作几乎都要 `sudo`（密码同为 8023）：

```bash
echo '8023' | sudo -S systemctl status ptz.service   # 查看服务状态
```

---

## 2. 服务管理

```bash
# 重启服务（部署代码后必做）
echo '8023' | sudo -S systemctl restart ptz.service

# 查看状态（是否 running、PID、CPU/内存）
echo '8023' | sudo -S systemctl status ptz.service

# 停止 / 启动
echo '8023' | sudo -S systemctl stop ptz.service
echo '8023' | sudo -S systemctl start ptz.service
```

### 查看日志

```bash
# 最近 50 行
echo '8023' | sudo -S journalctl -u ptz.service -n 50 --no-pager

# 实时跟踪（Ctrl+C 退出）
echo '8023' | sudo -S journalctl -u ptz.service -f

# 按时间段查
echo '8023' | sudo -S journalctl -u ptz.service --since '2026-09-11 06:00:00' --until '2026-09-11 07:00:00'

# 只看跟踪/异常关键日志
echo '8023' | sudo -S journalctl -u ptz.service -n 200 | grep -E '\[track\]|\[enc\]|\[filter\]|\[udp\]|\[reset\]'
```

---

## 3. 部署代码

代码在服务器 `/home/aap/ptz/`：

| 路径 | 说明 |
|------|------|
| `/home/aap/ptz/backend/main.py` | 后端主程序（Flask + waitress） |
| `/home/aap/ptz/backend/satellite.py` | 卫星跟踪逻辑 |
| `/home/aap/ptz/backend/auth.py` | 用户认证 |
| `/home/aap/ptz/frontend/` | 网页前端（index.html / satellite.js） |
| `/home/aap/ptz/users.json` | 网页登录账号（scrypt 哈希） |
| `/home/aap/ptz/config.json` | 配置（云台速度、TLE 源等） |
| `/home/aap/ptz/tle_cache.json` | TLE 卫星数据缓存 |

本地 `scp` 上传后重启：

```bash
scp main.py aap@192.168.31.70:/home/aap/ptz/backend/main.py
ssh aap@192.168.31.70 "echo '8023' | sudo -S systemctl restart ptz.service"
```

> 前端文件（index.html 等）是静态文件，上传后**无需重启**，刷新浏览器即可。

---

## 4. 常用 API 测试

服务端口：**8090**（网页/API）　**8091**（ESP8266 UDP 上报）。
登录后带 cookie 测试：

```bash
# 登录（网页账号 BH6AAP，密码见 users.json 或已重置为 8023）
curl -s -c /tmp/jar -X POST http://127.0.0.1:8090/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"BH6AAP","password":"8023"}'

# 以下命令统一加 -b /tmp/jar 保持登录
J="-b /tmp/jar"
```

### 4.1 状态查询

```bash
curl -s $J http://127.0.0.1:8090/api/status          # 服务/串口/UDP/暂停/复位状态
curl -s $J http://127.0.0.1:8090/api/encoder         # AS5600 编码器实时值(含固件版本 ver)
curl -s $J http://127.0.0.1:8090/api/position        # 云台推算位置
curl -s $J http://127.0.0.1:8090/api/sat/track/status # 是否在跟踪/跟踪哪颗
curl -s $J http://127.0.0.1:8090/api/seriallog       # 串口收发日志
curl -s $J http://127.0.0.1:8090/api/logs             # 调试日志面板数据
```

### 4.2 手动驱动测试（范例）

```bash
# 按住方向（持续转，直到发 stop）
curl -s $J -X POST http://127.0.0.1:8090/api/move/right
curl -s $J -X POST http://127.0.0.1:8090/api/move/stop

# 点动脉冲：向右转 500ms（俯仰用 up/down）
curl -s $J -X POST http://127.0.0.1:8090/api/pulse \
  -H 'Content-Type: application/json' -d '{"dir":"right","ms":500}'
curl -s $J -X POST http://127.0.0.1:8090/api/pulse \
  -H 'Content-Type: application/json' -d '{"dir":"up","ms":300}'

# 转到指定位置
curl -s $J -X POST http://127.0.0.1:8090/api/move/to \
  -H 'Content-Type: application/json' -d '{"pan":180,"tilt":45}'
```

### 4.3 复位与模式

```bash
# 复位：水平找光电零位 + 俯仰归零（约 20~40s）
curl -s $J -X POST http://127.0.0.1:8090/api/reset

# 紧急刹车（暂停所有转动）/ 恢复
curl -s $J -X POST http://127.0.0.1:8090/api/pause -H 'Content-Type: application/json' -d '{"paused":true}'
curl -s $J -X POST http://127.0.0.1:8090/api/pause -H 'Content-Type: application/json' -d '{"paused":false}'

# 开环控制模式（不信 AS5600，只用光电零位 + 航位推算）
curl -s $J -X POST http://127.0.0.1:8090/api/mode/open_loop -H 'Content-Type: application/json' -d '{"enabled":true}'
curl -s $J -X POST http://127.0.0.1:8090/api/mode/open_loop -H 'Content-Type: application/json' -d '{"enabled":false}'

# 防缠绕复位（按累计转动反向回退）
curl -s $J -X POST http://127.0.0.1:8090/api/anti_tangle_reset
```

### 4.4 卫星跟踪测试

```bash
# 查询 TLE 卫星（搜索）
curl -s $J "http://127.0.0.1:8090/api/sat/search?q=ISS"

# 收藏列表
curl -s $J http://127.0.0.1:8090/api/sat/favorites

# 开始跟踪（NORAD 编号）
curl -s $J -X POST http://127.0.0.1:8090/api/sat/track/25544

# 停止跟踪
curl -s $J -X POST http://127.0.0.1:8090/api/sat/track/stop

# 卫星实时位置（方位/仰角）
curl -s $J http://127.0.0.1:8090/api/sat/position/25544
```

### 4.5 光电校零与测速

```bash
# 光电自动校零+测速（云台自动转一圈，光电触发两次完成）
curl -s $J -X POST http://127.0.0.1:8090/api/encoder/photocalib \
  -H 'Content-Type: application/json' -d '{"mode":"auto"}'

# 手动设当前位置为 0° 基准
curl -s $J -X POST http://127.0.0.1:8090/api/encoder/setzero
```

---

## 5. 速度校准流程（换新云台/传动比时）

1. 网页"速度校准"卡片，或直接调 `/api/calibrate`：
   ```bash
   curl -s $J -X POST http://127.0.0.1:8090/api/calibrate \
     -H 'Content-Type: application/json' -d '{"direction":"right","duration":5000}'
   ```
2. 实测转过角度 → 更新 `config.json` 的 `pan_speed_dps` / `tilt_*_speed_dps`
3. 重启服务生效

> 速度标定直接影响航位推算精度（开环模式 + 闭环断流推算都依赖它）。

---

## 6. ESP8266 传感器

- IP：`192.168.31.66`（DHCP，可能有变，以服务器日志为准）
- 协议：UDP 广播 → 服务器 `8091` 端口，20Hz 上报角度 + 光电零位事件
- 固件版本：`/api/encoder` 返回 `ver` 字段

### 固件 OTA 更新

```bash
# HTTP OTA：浏览器或 curl 上传 .bin（口令 ptz2026）
curl -s -m 120 -F "token=ptz2026" \
  -F "sketch=@固件.bin" \
  http://192.168.31.66:8080/update
# 返回 "OK. Rebooting..." 即成功
```

> 网页入口：`http://192.168.31.66:8080`（上传页面）；ArduinoOTA 端口 8266（IDE 直传）。

---

## 7. 常见问题排查

| 现象 | 排查步骤 |
|------|----------|
| 服务起不来 | `journalctl -u ptz.service -n 30` 看 traceback；`python3 -m py_compile main.py` 查语法 |
| ESP32 显示离线 | `curl /api/encoder` 看 `time`/`now` 差；串口日志是否报 `UnboundLocalError` |
| 跟踪乱转 | 日志看 `[track] SLOW/watchdog`、`[encoder] 锚定`、`[filter] 忽略毛刺/基准更新` |
| 卫星列表空 | `/api/sat/tle` 看 count；`tle_cache.json` 是否有效；网络能否访问 TLE 源 |
| 网页登录不了 | `users.json` 账号；密码已重置为 8023；`curl /api/auth/login` 测 |