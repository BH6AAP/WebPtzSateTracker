# WebPtzSateTracker 开发记录

> 云台卫星跟踪系统：ESP32-S3(AS5600 磁编码器) → UDP → Python(Bottle/Waitress) 后端 → Web 前端
> 本文档用于交接开发上下文，新任务先读本文 + `../j1900主机说明.md`（连接凭据）。

## 1. 项目结构

```
WebPtzSateTracker/
├── backend/main.py            # 后端主程序（单文件，含所有接口与逻辑）
├── frontend/index.html        # 前端页面（含内联 JS）
├── frontend/satellite.js      # 前端卫星/编码器面板脚本
├── esp32s3_as5600/
│   ├── main.py                # ESP32 MicroPython 固件
│   ├── wifi_manager.py        # WiFi 配网/重连
│   ├── udp_verify.py          # 本机 UDP 监听验证脚本（python3 直接跑）
│   └── raw_udp_mon.py         # raw 实时监控脚本
└── 开发记录.md                # 本文档
```

## 2. 部署信息

| 项 | 值 |
|----|-----|
| 服务器 | 192.168.31.67（Ubuntu），SSH `aap`/`8023`，sudo 同密码 |
| 服务 | `ptz.service`（systemd），代码 `/home/aap/ptz/{backend,frontend}` |
| 本地访问 | `http://192.168.31.67:8090` |
| ESP32 | 上报 UDP 广播端口 **8091**（100Hz）；WiFi IP 现为 192.168.31.106 |

部署命令（在项目根目录）：
```bash
sshpass -p '8023' scp backend/main.py aap@192.168.31.67:/home/aap/ptz/backend/main.py
sshpass -p '8023' scp frontend/index.html aap@192.168.31.67:/home/aap/ptz/frontend/index.html
sshpass -p '8023' ssh aap@192.168.31.67 "echo 8023 | sudo -S systemctl restart ptz.service && sleep 3 && curl -s http://127.0.0.1:8090/api/encoder"
```

## 3. 核心机制

### 3.1 AS5600 角度（关键：不要用 raw 计算）
- **raw**：AS5600 单圈 0~4095，循环（0→4096 跳变），**只作诊断显示，绝不参与角度计算**
- **angle**：ESP32 固件 `unwrap()` 累加得到的**有符号多圈累积角**（`_total_abs`），可正可负，跨重启持久化（`as5600_state.txt`）
- **关系**：云台输出轴 1 圈 = AS5600 4 圈（**4:1 减速**），即 `物理角 = angle / 4`
- 固件 unwrap 已做 ±180° 差分截断抗抖动；重启**不恢复**旧 `_last_angle`（只恢复 `_total_abs`），否则累积失效
- 光电零位事件：`{"event":"zero","raw":X,"angle":XX,"tick":N}`，带触发瞬间的累积角

### 3.2 后端角度计算（统一物理角域）
- `zero_angle`：光电 0° 对应的 ESP32 累积角（持久化 `encoder_cal.json`，物理 0 基准=光电位）
- **传动比 4:1**：ESP32 累积角 = 物理角 × 4，后端换算统一除以 4（`ENCODER_GEAR`）
- `cont_angle = (angle - zero_angle) / 4`（物理连续角，防缠绕判定依据，单位=物理角）
- `pan = (angle - zero_angle) / 4 % 360`（显示用 0~360°，360°=0°=光电零位，顺时针增/逆时针减，不用负值）
- 接口字段：`{angle, raw, pan, cont_angle, zero_angle, tangle_warn}`（`ok()` 展开在顶层，**无 data 字段**）

### 3.3 防缠绕判定（物理角域）
- 范围：`cont_angle ∈ [-540, 540]`（物理，左右各 1.5 圈），中心 0 = 光电零位（物理 0°）
- `_tangle_warn()`：越界返回告警文本，写入 `_encoder_state["tangle_warn"]`，经 `/api/encoder`、`/api/status` 暴露
- 前端：顶部状态点橙色闪烁 + toast（`index.html pollEncoder`）

### 3.4 复位归中（`/api/reset`）
流程：停止任务 → **超限回转检测** → 低速转圈找光电零位（`_reset_loop`）→ `_reanchor_zero()` 锚定 → **直接停在物理 0°（光电零位）**
- **超限回转（`_recover_from_tangle`）**：基于**回传角度 `angle` 与标定零位 `zero_angle` 的偏移**（ESP32 域 `off = angle - zero_angle`）判断，不依赖 `cont_angle` 换算
  - 物理范围 [-540,540] 对应 ESP32 域 [-2160,2160]（×4）；`off>2160`（向右超限）或 `off<-2160`（向左超限）
  - **方向试探（`_probe_recover_direction`）**：不硬编码 left/right——先向 left 驱动 0.6s，比较 `|off|` 是否收敛；收敛用 left，发散改用 right（防该云台 left/right 实际转向与标准帧相反）
  - 用回传角度闭环：`off` 回到 `0°ESP±40°` 即停；驱动 2.5s 内偏移变化 < 8°ESP（物理 2°）判定**堵转**（线缆缠死），立即停止并返回 `abort=True`，前端 toast 提示"回不去"，不执行复位
  - 回转超时 240s 兜底
- 找零方向同用试探结果（`_reset_loop` 内 `_probe_recover_direction`），避免朝反方向绕远（曾出现复位从 angle 6800 绕到 8300 才找到光电零位）
- **防缠绕累计基于 ESP32 域**：`pan_accum` 增量直接用 `off = angle - zero_angle`（物理0°映射的 ESP32 记录角），不再除以 4；范围 `TANGLE_MIN/MAX = ±2160°ESP`（物理 ±540°），与超限回转同域；前端显示换算物理角（÷4）
- 归中后 `pan=0`，`zero_angle` 不变，`pan_accum` 清零
- `/api/reset` 返回 `duration`（含俯仰归零 + 找零时长）；`abort=True` 表示超限回不去

### 3.5 光电自动校零（`/api/encoder/photocalib`）
- 全速右转，光电触发两次（起点 0°+一圈后终点），用两次触发的累积角增量算传动比 `as5600_revs`，`end_angle` 作为 `zero_angle` 锚点
- 可测速：`pan_speed_dps = 360/(t1-t0)`

### 3.6 卫星跟踪
- `SatelliteTracker`，`_pan_delta` 最短路径双向旋转；**水平无限位**（仅俯仰物理限位）
- 跟踪中经过光电零位：`_calibrate_zero_tracking` 偏差<15° 平滑校准 zero_angle

## 4. 已知关键坑

- `ok()` 返回 `{"ok":True, **data}` —— 前端读顶层字段，不要用 `d.data`
- 未标定时 `_process_encoder_data` 仍须写入 `angle`/`raw`（否则接口一直 null）—— 已修复
- 手动转动经过光电零位**不得**重锚定 0 点（绝对 0 由 zero_angle 固定）—— 已修复
- 后端 UDP 监听端口 8091 与 ESP32 广播必须同网段；`SO_REUSEADDR` 可与其他监听共存
- ESP32 AS5600 曾出现 MD=0（检测不到磁铁）→ 重新焊接恢复；告警阈值已降低（只对 MD=0/MH=1 告警，忽略 ML=1）

## 5. 最近完成工作（本次会话）

1. 后端从 raw 域重构为 angle 域：删除 `As5600Raw`、`raw_per_deg`、旧标定接口
2. 修复 `/api/encoder` 返回格式（前端 `d.data`→`d`）
3. 修复未标定时 angle/raw 不更新 bug
4. 新增防缠绕判定 + 越界告警（前后端）
5. 新增复位归中（找零后停在物理 0°）
6. ESP32 固件已稳定（unwrap 累积、光电 zero 事件带 angle、状态恢复）
7. **角度映射修复**：后端 pan/cont_angle 统一除以 4 换算为物理角（之前漏除导致物理 90° 显示 0°）
8. **复位目标修复**：复位停在物理 0°（光电零位），删除原先再正转 540° 的 `_center_after_reset`
9. **pulse 双重减零位修复**：cont_angle 已是物理连续角，不再二次 `_pan_cont_from_angle`
10. 防缠绕范围改为物理角域 [-540, 540]
11. **复位超限回转**：`_recover_from_tangle` 自动往 0° 回转，堵转(2.5s cont 无变化)判定回不去，返回 `abort=True` 提示

## 6. 待办/待验证

- [ ] 实测 `/api/reset`：找零后停在物理 0°，pan=0
- [ ] 实测角度映射：物理 90° 时前端 pan 应显示 90°
- [ ] 实测防缠绕越界告警触发（手动转到物理 >540° 或 <540°）
- [ ] 实测超限复位：超限时自动回转回 0°，堵转时提示"回不去"
- [ ] 验证光电校零完成后 pan 归 0° 且传动比约 4:1
- [ ] 验证手动转动经过光电零位不被重置

## 7. 常用接口速查

| 接口 | 方法 | 作用 |
|------|------|------|
| `/api/encoder` | GET | 编码器状态（angle/raw/pan/cont_angle/zero_angle/tangle_warn） |
| `/api/encoder/photocalib` | GET/POST | 光电自动校零+测速 |
| `/api/reset` | POST | 复位（找零+停在物理 0°） |
| `/api/anti_tangle_reset` | POST | 防缠绕复位（按 pan_accum 反向回退） |
| `/api/position` | GET | 当前 pan/tilt/pan_accum |
| `/api/status` | GET | 在线状态/udp_online/tangle_warn |
| `/api/move/to` | POST | 转到目标角度 |
| `/api/moon/position` | GET | 月球实时方位/仰角/距离（Meeus 简化算法，~0.3°） |
| `/api/sat/track/moon` | POST | 启动月球跟踪（含俯仰归零流程） |
| `/api/sat/track/status` | GET | 跟踪状态（含 target: sat/moon） |
| `/api/settings` | POST | 支持 `tle_urls` 数组（TLE 数据源，空=默认 AMSAT+CelesTrak） |
| `/api/sat/tle/refresh` | POST | 手动强制更新 TLE（前端设置区有按钮） |

## 8. 2026-09-07 新增功能

1. **月球跟踪**：`satellite.moon_position()`（Meeus 主要摄动项算法，ECEF 差分处理站心视差，复用 GMST/azel 链）；`SatelliteTracker.start(norad, target="moon")` 目标函数化，复用预测性闭环跟踪
2. **TLE 数据源设置**：config.json `tle_urls` 数组，`satellite.get_tle_urls()` 读取（空=默认源）；前端设置区 textarea 编辑
3. **手动更新 TLE**：设置区"⬇ 立即更新 TLE 数据"按钮 + 缓存卫星数显示
4. **收藏列表**：后端/前端原本已有（本次未改动）
5. 注意：月球跟踪norad固定为 `"moon"`，track/status 用 `target` 字段区分；前端 `setTrackingUI` 统一管理跟踪 UI 状态
